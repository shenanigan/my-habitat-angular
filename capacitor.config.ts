import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rc.myhabitat',
  appName: 'my-habitat',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
